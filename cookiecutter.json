{
  "project_name": "My Company",
  "project_slug": "{{ cookiecutter.project_name.lower()|replace(' ', '')|replace('-', '_')|replace('.', '_')|trim() }}",
  "domain": "{{cookiecutter.project_slug}}.localhost",
  "use_waautomate": "y",
  "use_traefik": "y",
  "use_letsencrypt": "y",
  "include_rocketchat": "y",
  "include_metabase": "y"
}
